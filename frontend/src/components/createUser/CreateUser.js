import React, { useState } from "react";
import UserService from "../services/UserService";
import {Button, Input} from 'antd';

const CreateUser = () => {
    return (
        <>
            <Button className="buttonLoggin" type="primary"></Button>
        </>
    );
};

export default CreateUser;